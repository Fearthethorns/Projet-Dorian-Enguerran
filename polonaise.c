#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#define CAPACITE 10000
#define TAILLE 10

void pousser(char tableau[]);
    int x;
    tableau[0]=x;
    tableau[0]=tableau[sizeof(tableau)];
    tableau[sizeof(tableau)]=x;

int main(int argc,char*argv[]){
    char sysraw[];
    scanf("%s",sysraw);
    int taille=sizeof(sysraw);
    int compteur=0
    int elementgauche=0;
    int elementdroite=0;
    for(compteur,compteur<taille,compteur++){
        if('sysraw[compteur]'=='+'){
            
        }
    }
}